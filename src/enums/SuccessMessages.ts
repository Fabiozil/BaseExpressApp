export enum ESuccessMessage {
    HEALTH_CHECK_OK = "HEALTH_CHECK_OK",
}

export enum ESuccessCode {
    HEALTH_CHECK_OK = 200000,
}

export enum EDefaultSuccessDetail {
    SUCCESS_RESPONSE = "Success Response",
    UNKNOWN_RESPONSE = "Unknown Success Response",
}
